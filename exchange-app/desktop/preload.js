// Intentionally minimal: the app is a self-contained web page with no need
// for Node/filesystem access, so nothing is exposed to the renderer.
// contextIsolation stays on and nodeIntegration stays off in main.js.
